import { DefaultComponent as ImportedComponent } from '../../util/default-component/index';

export const FlexParent = ImportedComponent;

export const FlexChild = ImportedComponent;

export const Flex = { Parent: FlexParent, Child: FlexChild };

export default Flex;
